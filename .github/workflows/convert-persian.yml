name: Convert Persian Translations ODT files

on:
  push:
    paths:
      - 'translations/Persian/*.odt'

jobs:
  convert:
    uses: ./.github/workflows/_convert-odt-base.yml
    with:
      folder: 'translations/Persian'
      # No extra fonts - just like the original script
    secrets: inherit
