name: Convert Manifest ODT files

on:
  push:
    paths:
      - 'manifest/*.odt'

jobs:
  convert:
    uses: ./.github/workflows/_convert-odt-base.yml
    with:
      folder: 'manifest'
    secrets: inherit
